<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<OutputType>WinExe</OutputType>
		<TargetFramework>net8.0-windows</TargetFramework>
		<UseWPF>true</UseWPF>
		<!-- Включает Windows Forms для FolderBrowserDialog (диалог выбора папки). Зачем: чтобы выбирать папки без внешних библиотек. -->
		<UseWindowsForms>true</UseWindowsForms>
	</PropertyGroup>
</Project>